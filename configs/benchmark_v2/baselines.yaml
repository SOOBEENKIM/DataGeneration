extends: main.yaml
model_seeds: [1, 2, 3, 4, 5]
sampling_seed_offset: 1000
bootstrap_seed_offset: 2000
ctgan: {epochs: 30, batch_size: 500}
tvae: {epochs: 30, batch_size: 500}
markov: {laplace_alpha: 1.0, amount_model: empirical}
