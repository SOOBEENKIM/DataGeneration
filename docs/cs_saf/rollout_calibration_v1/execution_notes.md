# 실행 기록

- 사전등록: `13dc0ac`. 탐색·선택·평가 난수, 10개 보정값, 보호 한도, 24회 탐색 및 종료 기준을 구현·실행 전에 고정했다.
- 학습 구현: `027bc55`. 사전 CPU/GPU 검증 기록: `429172e`. 첫 과학적 탐색은 검증 후 시작했다.
- 최종 평가·oracle·판정·독립 산술 확인 코드는 `1417169`에서 고정했다. 그 시점에는 보정 탐색 중이며 최종 평가를 실행하지 않았다. 표·그림 생성기는 `0b27051`에서 추가했다.
- CPU 검증은 train 관측 빈도의 독립 재계산, NLL 차이의 전체 행동 확률식과 일치, 훈련용 두 계획의 개체 분리, 61회 탐색/4회 선택 호출, 부적격 후보 거부, 동결 및 같은 난수 재현을 확인했다. U/G 모두 순차 hidden 및 행동 확률이 전체 이력 계산과 CPU에서 정확하게 같았다.
- GPU 검증에서 전체 이력 계산과 순차 계산의 최대 행동 확률 차이는 U 1.19×10⁻⁷, G 1.79×10⁻⁷이었다. 같은 생성 이력의 CPU/GPU 행동 확률 최대 차이는 각각 1.19×10⁻⁷이었다. FP32, TF32 off, deterministic algorithms를 사용했다.
- GPU 한 작업씩, 매 부모 실행 전 여유≥2GiB·사용률≤65%를 확인했다. allocator 상한은 4%이며 다른 작업을 종료하지 않는다. 최초 검증의 전체 계획 생성은 약 1.42초, 최대 예약 메모리는 56MiB였다. CUDA context 등 드라이버 메모리는 이 예약 메모리 값에 포함되지 않는다.
- 과학적 보정은 GPU에서만 실행한다. 모든 보정이 저장된 뒤 최종 생성·oracle·검증을 CPU에서 실행하며 평가 worker 최대 2개, 각 1 thread다. CPU/GPU 확률이 일치하는 것은 확인했지만 부동소수점 차이까지 포함해 서로 다른 장치에서 모든 이산 경로가 bitwise 같다고 주장하지 않는다. 최종 A/B/P는 모두 같은 CPU 생성기를 사용한다.
- B/P의 생성 호출 예산은 동일하다. A는 기존 실제 이력 NLL 최적화의 수렴 보정을 재사용하므로 A/B/P의 총 계산비용까지 같지는 않다. 새 학습 비용을 따로 보고한다. 신경망 파라미터와 보정 용량은 같다.
- 3개 고정 탐색 step 때문에 각 좌표는 초기 A에서 최대 .7까지만 움직일 수 있다. 절대 범위 ±8도 유지된다. 실패는 이 한정된 탐색의 결과이며, 모든 생성 관계 학습법이 불가능함을 입증하지 않는다.
- 평가 전에 학습 결과를 고정하는 barrier와 각 평가 manifest의 시작 시각을 확인한다. 정답 생성기 코드는 평가 함수 안에서만 불러오며 학습 목표나 선택에 사용하지 않는다.

최종 완료 여부와 원본 목록은 실행 후 만들어지는 `execution_summary.json`, 검증 결과는 `verification.json`을 기준으로 확인한다. 과거 C의 실패와 과거 E/ER 결과는 변경하지 않는다.
