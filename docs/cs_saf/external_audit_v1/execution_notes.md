# 실행상의 확인과 수정

- 원래 환경은 변경하지 않고 별도 Python 환경을 설치했다. 공식 2.4.0 소스를 사용했으며 신경망·손실·최적화는 수정하지 않았다. 2.7.0 최신 HEAD를 실행했다고 하지 않는다.
- CPU 학습·checkpoint 재로드·생성·인코딩 왕복 검증 통과. 진단 네트워크의 현재 정답/미래 정보 불변성과 prefix 일치도 통과했다.
- 초기 CPU 진단 호출의 wildcard import에서 비공개 함수 이름이 제외되어 NameError가 났다. 명시적 import로 바로잡았다. 학습/평가 수치와 무관한 호출 오류다.
- 첫 저장 모델의 읽기 전용 replay에서 Pandas nullable string의 첫 행 비교가 NA여서 정수 변환이 실패했다. 첫 거래의 반복 여부는 정의되지 않고 모든 관계 평가에서 제외된다. 그 구조적 첫 비교만 False로 표시해 직렬화를 가능하게 수정했다. 원래 생성값·모델·학습 조건은 바꾸지 않았다. 실패 log를 보존하고 replay만 재실행한다.
- 공식 순차 손실의 정규화는 배치 마스크 총합을 사용하므로 로그 NLL의 절대 크기를 U/E 또는 다른 batch size와 비교하지 않는다. 같은 설정의 공식 checkpoint 선택을 그대로 사용했다.
- 사전등록한 시간/epoch 상한을 그대로 유지한다. 상한에서 종료되면 최적화 미완료 가능성을 별도 기재하며 구조적 불가능성을 주장하지 않는다.
- 첫 조건에서 큰 반복률 오차가 확인된 뒤, 해석을 위해 반복률의 평균 수준 오차와 평균을 제거한 곡선 모양 오차를 추가 요약한다. 이는 사후 탐색적 분해이며 사전등록 주 지표나 성공 기준을 바꾸지 않는다. L1의 두 성분 크기는 가법 분해가 아니다.
- 완료된 첫 학습에서 전체 GPU 점유가 약 805MiB인 시점이 확인되었다. 이후 외부 작업이 메모리를 점유해 남은 2회가 대기했다. 이 두 실행은 등록 상한 25%보다 더 작은 **4% PyTorch allocator 상한(약 983MiB, 드라이버 메모리 제외)**을 적용하고, 최소 2GiB 여유가 있으며 상대적으로 사용률이 낮은 GPU에서만 시작하도록 대기 조건을 조정한다. 모델·batch·epoch·시드·목적함수는 바꾸지 않는다. 다른 작업은 중지하지 않으며, 자체 OOM 발생 시 그 실행을 기술 실패로 보존하고 추가 실행을 멈춘다.
- 등록 4회 모두 완료했다. 마지막 두 모델의 기록된 PyTorch 최대 reserved 메모리는 각각 446,693,376 bytes이며 OOM은 없었다. 앞선 두 모델에는 이 최대값 기록 기능이 없으므로 누락값을 0으로 해석하지 않는다. 이번 작업의 학습·replay 프로세스는 종료했다.
- 실제 이력에서도 큰 반복 오류가 나온 뒤, train 평균 반복률의 상수 예측과 Brier/NLL을 비교하는 사후 sanity control을 추가했다. 신경망 재학습이나 생성 모델 추가가 아니다. 4개 모델 모두 이 단순 반복 예측보다 Brier 오차가 컸다.
- 최종 보관 검증에서 원본/adapter hash와 순서·값, 공식 내부 train+validation을 합친 인코딩 반복률의 보존, 4개 checkpoint hash, 20개 생성물의 개체·행동 support·유한 수치값을 확인했다. 반복곡선 L1을 독립 계산한 값은 기존 지표와 최대 5.56e-17 차이였다.
- 제약 결합 후보의 확률 계산 구성요소와 반례 계산은 CPU에서만 검증했다. 새로운 완성 모델의 학습으로 세지 않는다. 원래의 넓은 설계 메모보다 최신 `research_decision.md`의 좁힌 후보·진행 조건을 우선한다.
